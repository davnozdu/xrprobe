# Протокол XREAL One Pro

Разобран перехватом USB (`usbmon`) на OnePlus 15 с Android 16 и проверен
воспроизведением: сгенерированные кадры совпадают с перехваченными побайтово.

Устройство: **VID `0x3318`, PID `0x0436`**, USB 2.0 high speed.

## Интерфейсы

Без камеры — 9 интерфейсов, после активации Eye — 11.

| Интерфейс | Класс | Назначение |
|---|---|---|
| 1.0 | HID vendor `FF00` | **канал управления**, interrupt IN/OUT по 1024 байта |
| 1.1 / 1.2 | CDC NCM | USB-сеть (`eth0`), только DHCP и заглушечный DNS |
| 1.3 / 1.4 | CDC ECM | USB-сеть (`eth1`), то же |
| 1.5–1.7 | Audio | динамики и микрофон, 48 кГц, стерео, S32_LE |
| 1.8 | HID | мышь, медиа-клавиши, system control |
| 1.9 / 1.10 | Video (UVC) | **появляются только после активации камеры** |

## Формат кадра

```
fd | CRC32-LE (4 байта) | тело
```

Тело:

```
длина (2, LE) | 8 нулей | код (1) | 6 нулей | данные
```

Поле длины считает байты от себя до конца кадра. Полная длина = 5 + длина.

**CRC32** — стандартный: полином `0xEDB88320`, начальное значение
`0xFFFFFFFF`, финальная инверсия. Считается **от начала тела** (смещение 5)
до конца кадра, записывается little-endian.

Контрольная сумма зависит только от содержимого: одна и та же команда в
разных перехватах шла с одинаковым CRC. Порядковых номеров в кадре нет.

## Известные команды

| Код | Назначение | Данные |
|---|---|---|
| `0x26` | версия прошивки | — (в ответе ASCII, например `15.1.03.5`) |
| `0xd2` | статус | — |
| `0xd3` | **запись конфигурации USB** | `45 10 01 00` — включает UVC |
| `0xd4` | чтение конфигурации USB | — (ответ: `04 00 00 00` до активации) |
| `0xd6` | чтение свойства | ASCII-имя, например `ro.bsp.app` |

## Активация камеры

```
fd c6 e4 89 b8 15 00 00 00 00 00 00 00 00 00 d3 00 00 00 00 00 00 45 10 01 00
```

После записи в `/dev/hidraw0` устройство **переподключается по USB** и
поднимается с интерфейсами 1.9 / 1.10, которые подхватывает штатный
ядерный `uvcvideo`. Появляется `/dev/video2`.

Отдельной команды «запустить поток» нет: приложение XREAL каждый раз
шлёт ту же `0xd3`, и взведение происходит за счёт переэнумерации.

**Ограничение:** если камера уже активирована, повторная `0xd3` ничего не
меняет и поток не взводится — UVC отвечает `ETIMEDOUT` на probe/commit.
Нужна деактивация с последующей активацией; команда деактивации пока
не подтверждена.

## Камера

Фоторежима нет: `bStillCaptureMethod = 0`, дескриптор `VS_STILL_IMAGE_FRAME`
отсутствует. Доступен только видеопоток.

| Формат | Разрешения | Заявленный битрейт |
|---|---|---|
| HEVC | 2048×1512, 1920×1080 | 248–496 Мбит/с |
| MJPEG | 1920×1080, 1080×1920, 720×1280 | 166–332 Мбит/с |

Заявленный буфер кадра MJPEG — 4 147 200 байт. Фактически приходит
60–75 КБ на кадр: размер следует за детализацией сцены, обрезки нет
(JPEG приходит с корректным маркером `ff d9`).

### Регуляторы

Регулятора качества сжатия нет. Доступны:

| Регулятор | ID | Диапазон | По умолчанию |
|---|---|---|---|
| Focus, Absolute | `0x009a090a` | 200–800 | 300 |
| Sharpness | `0x0098091b` | 0–100 | 50 |
| Auto Exposure | `0x009a0901` | 0–3 | 0 |
| Exposure Time | `0x009a0902` | 1–300 | 0 |
| Zoom, Absolute | `0x009a090d` | 100–200 | 100 |
| Brightness / Contrast / Saturation / Hue | `0x00980900`+ | 0–100 | 50 |
| White Balance Temperature | `0x0098091a` | 2800–6800 | 5000 |
| Power Line Frequency | `0x00980918` | 0–2 | 3 (вне диапазона) |

Фокус ручной. Его положение на резкость влияет сильнее битрейта.

## Аудио

Работает независимо от камеры и от DisplayPort. Очки видны Android как
`usb_headset`, разговорный тракт идёт через них (на Qualcomm — по пути
USB audio offload, мимо ядерной ALSA, поэтому `/proc/asound` показывает
`Stop`, а `usbmon` не видит трафика).

Отключение внешнего дисплея звук **не ломает**:

```sh
echo off > /sys/class/drm/card0-DP-1/status    # монитор исчезает, звук остаётся
echo detect > /sys/class/drm/card0-DP-1/status # вернуть
```
